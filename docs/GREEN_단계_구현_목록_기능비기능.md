# GREEN 단계 구현 목록
## 기능/비기능 요구사항 분류

**작성 일자**: 2024년 12월 19일  
**브랜치**: GREEN  
**참고 문서**: 
- RED 테스트 케이스 (TC-RED-001)
- RED 테스트 실행 리포트
- GREEN 단계 체크리스트

---

## 📋 개요

RED 단계에서 테스트한 결과를 바탕으로 GREEN 단계에서 구현해야 할 항목을 **기능 요구사항**과 **비기능 요구사항**으로 분류했습니다.

### 현재 상태
- ✅ **완료된 기능**: TC-RED-001 (시뮬레이터 실시간 계산)
- ❌ **미완료 기능**: 21개
- **현재 커버리지**: 16% (4/25 기능)

---

## 🔧 기능 요구사항 (Functional Requirements)

기능 요구사항은 사용자가 시스템을 통해 수행할 수 있는 구체적인 기능입니다.

### 1. 대시보드 기능

#### 1.1 현재 상태 요약 영역 입력 기능
- [ ] **현재 나이 입력 필드**
  - 입력 필드 구현 (`id="currentAge"`)
  - 숫자 입력 검증
  - 값 저장 기능

- [ ] **목표 은퇴 나이 입력 필드**
  - 입력 필드 구현 (`id="retirementAge"`)
  - 숫자 입력 검증
  - 값 저장 기능

- [ ] **현재 직업/상태 입력 필드**
  - 입력 필드 구현 (`id="currentJob"`)
  - 텍스트 입력
  - 값 저장 기능

- [ ] **월 평균 수입 입력 필드**
  - 입력 필드 구현 (`id="monthlyIncome"`)
  - 숫자 입력 검증
  - 값 저장 기능

- [ ] **월 평균 지출 입력 필드**
  - 입력 필드 구현 (`id="monthlyExpense"`)
  - 숫자 입력 검증
  - 값 저장 기능

#### 1.2 자산 & 수입 요약 계산 기능
- [ ] **자산 카테고리 입력 및 합계 계산**
  - 현금/예금 입력 필드 (`id="cashDeposit"`)
  - 연금 입력 필드 (`id="pension"`)
  - 기타 자산 입력 필드 (`id="otherAssets"`)
  - 자산 합계 자동 계산 함수 (`calculateTotalAssets()`)
  - 합계 표시 영역 (`id="totalAssets"`)

- [ ] **수입 카테고리 입력 및 합계 계산**
  - 근로 소득 입력 필드 (`id="laborIncome"`)
  - 기타 소득 입력 필드 (`id="otherIncome"`)
  - 수입 합계 자동 계산 함수 (`calculateTotalIncome()`)
  - 합계 표시 영역 (`id="totalIncome"`)

- [ ] **지출 카테고리 입력 및 합계 계산**
  - 고정 지출 입력 필드 (`id="fixedExpense"`)
  - 생활비 입력 필드 (`id="livingExpense"`)
  - 지출 합계 자동 계산 함수 (`calculateTotalExpense()`)
  - 합계 표시 영역 (`id="totalExpense"`)

#### 1.3 단계별 노후 계획 기능
- [ ] **4개 단계 카드 표시**
  - 1단계: 50~55세 카드
  - 2단계: 56~60세 카드
  - 3단계: 61~65세 카드
  - 4단계: 66세+ 카드

- [ ] **각 단계별 메모 입력 기능**
  - 각 단계별 메모 입력 영역 (최대 3줄)
  - 메모 저장 기능
  - 메모 로드 기능

#### 1.4 건강 & 생활 관리 기능
- [ ] **정기검진 체크박스**
  - 체크박스 구현
  - 체크 상태 저장 기능

- [ ] **운동 목표 입력**
  - 입력 필드 구현
  - 값 저장 기능

- [ ] **생활 습관 메모**
  - 텍스트 영역 구현
  - 메모 저장 기능

- [ ] **취미/여행 계획 입력**
  - 입력 영역 구현
  - 값 저장 기능

#### 1.5 메모 & 할 일 관리 기능
- [ ] **올해의 핵심 목표 입력**
  - 입력 영역 구현
  - 값 저장 기능

- [ ] **다음 점검 항목 입력**
  - 입력 영역 구현
  - 값 저장 기능

- [ ] **자유 메모 입력**
  - 텍스트 영역 구현
  - 메모 저장 기능

### 2. 데이터 저장 및 복원 기능

#### 2.1 대시보드 데이터 저장 기능
- [ ] **localStorage 저장 기능**
  - 저장 키 정의 (`retirement_dashboard_data`)
  - `saveData()` 함수 구현
  - 모든 입력 필드 값 수집
  - JSON 형식 변환 및 저장
  - 입력 필드 변경 시 자동 저장

- [ ] **localStorage 로드 기능**
  - `loadData()` 함수 구현
  - localStorage에서 데이터 읽기
  - JSON 파싱
  - 각 입력 필드에 값 복원
  - 에러 처리 (try-catch)
  - 페이지 로드 시 자동 실행

- [ ] **데이터 동기화**
  - 데이터 로드 후 합계 재계산
  - 데이터 로드 후 유효성 검사

#### 2.2 시뮬레이터 데이터 저장 기능
- ✅ **이미 구현 완료** (TC-RED-001 통과)
  - localStorage 저장 기능
  - 데이터 로드 기능
  - 자동 저장/복원

### 3. 네비게이션 기능

#### 3.1 페이지 이동 기능
- [ ] **네비게이션 바 구현**
  - 대시보드 링크 (`href="/"`)
  - 시뮬레이터 링크 (`href="/simulator"`)
  - 링크 클릭 시 페이지 이동

#### 3.2 현재 페이지 표시 기능
- [ ] **활성 링크 표시**
  - 현재 페이지 감지 로직
  - 활성 링크에 `active` 클래스 추가
  - 활성 링크 스타일 적용

### 4. 입력 검증 및 엣지 케이스 처리 기능

#### 4.1 대시보드 입력 검증
- [ ] **음수 입력 처리**
  - HTML input 필드에 `min="0"` 속성 추가
  - JavaScript 유효성 검사
  - 음수 입력 시 경고 메시지 또는 0으로 처리

- [ ] **빈 값 입력 처리**
  - 빈 값 입력 시 0으로 처리 (`|| 0` 패턴)
  - 합계 계산 시 빈 값 처리 확인

- [ ] **매우 큰 숫자 입력 처리**
  - 입력 값 범위 제한 (필요 시)
  - 큰 숫자 표시 포맷팅 (억, 조 단위)

#### 4.2 시뮬레이터 입력 검증
- ✅ **이미 구현 완료** (TC-RED-001 통과)
  - 현재 나이 ≥ 은퇴 나이 오류 처리
  - 음수 입력 차단
  - 빈 값 처리

### 5. 계산 기능

#### 5.1 실시간 계산 기능
- ✅ **이미 구현 완료** (TC-RED-001 통과)
  - 시뮬레이터 실시간 계산
  - 입력 즉시 결과 업데이트

#### 5.2 자동 합계 계산 기능
- [ ] **자산 합계 자동 계산**
  - 입력 필드 변경 시 자동 계산
  - 계산 결과 포맷팅

- [ ] **수입 합계 자동 계산**
  - 입력 필드 변경 시 자동 계산
  - 계산 결과 포맷팅

- [ ] **지출 합계 자동 계산**
  - 입력 필드 변경 시 자동 계산
  - 계산 결과 포맷팅

---

## 🎨 비기능 요구사항 (Non-Functional Requirements)

비기능 요구사항은 시스템의 품질 속성, 제약사항, 사용성 등을 정의합니다.

### 1. 사용성 (Usability)

#### 1.1 UI/UX 사용성
- [ ] **5분 이내 현재 상태 파악 가능**
  - 레이아웃 최적화
  - 섹션별 명확한 구분
  - 중요한 정보 상단 배치
  - 정보 계층 구조 명확화

- [ ] **가독성 개선**
  - 섹션 제목 강조 (font-size, font-weight)
  - 입력 필드와 레이블 명확한 연결
  - 계산 결과 강조 표시

#### 1.2 글자 크기
- [ ] **50대 사용자 기준 글자 크기**
  - 본문 텍스트: 최소 16px
  - 제목: 24-32px
  - 입력 필드 텍스트: 16px 이상
  - 계산 결과: 18-20px (강조)

#### 1.3 색상 스키마
- [ ] **색상 최소화 원칙 준수**
  - 흑백 기본 색상 사용
  - 포인트 색상 1개만 사용 (파란색 #2563eb)
  - 색상 대비 확인 (접근성)

### 2. 레이아웃 및 디자인

#### 2.1 레이아웃 구조
- [ ] **섹션 구분 명확화**
  - 섹션 간 여백 조정
  - 섹션 제목 스타일 통일
  - 섹션 배경색 또는 border 추가

- [ ] **정보 과다 방지**
  - 불필요한 정보 제거
  - 중요 정보 강조
  - 계층 구조 명확화

#### 2.2 반응형 디자인
- [ ] **미디어 쿼리 구현**
  - 태블릿 레이아웃 (768px ~ 1024px)
  - 모바일 레이아웃 (< 768px)

- [ ] **반응형 레이아웃 조정**
  - 데스크톱: 그리드 레이아웃 (3-4열)
  - 태블릿: 그리드 레이아웃 (2열)
  - 모바일: 1열 레이아웃

- [ ] **반응형 요소 조정**
  - 각 화면 크기에서 레이아웃 확인
  - 입력 필드 크기 조정
  - 텍스트 가독성 확인

### 3. 브라우저 호환성 (Compatibility)

#### 3.1 주요 브라우저 호환성
- [ ] **Chrome 브라우저 호환성**
  - 모든 기능 정상 작동 확인
  - localStorage 정상 작동 확인
  - CSS 스타일 정상 렌더링 확인
  - JavaScript 오류 없음 확인

- [ ] **Edge 브라우저 호환성**
  - 모든 기능 정상 작동 확인
  - localStorage 정상 작동 확인
  - CSS 스타일 정상 렌더링 확인
  - JavaScript 오류 없음 확인

#### 3.2 추가 브라우저 호환성
- [ ] **Firefox 브라우저 호환성**
  - 모든 기능 정상 작동 확인
  - CSS 호환성 확인
  - JavaScript 호환성 확인

- [ ] **Safari 브라우저 호환성**
  - 모든 기능 정상 작동 확인
  - 이슈 수정

#### 3.3 호환성 이슈 처리
- [ ] **폴리필 추가** (필요 시)
  - 발견된 호환성 문제 수정
  - 브라우저별 특수 처리

### 4. 성능 (Performance)

#### 4.1 계산 성능
- ✅ **이미 구현 완료** (TC-RED-001 통과)
  - 실시간 계산 지연 없음
  - 즉시 결과 업데이트

#### 4.2 데이터 저장 성능
- [ ] **localStorage 저장 성능**
  - 저장 속도 확인
  - 대용량 데이터 처리 (필요 시)

### 5. 접근성 (Accessibility)

#### 5.1 기본 접근성
- [ ] **명확한 레이블**
  - 모든 입력 필드에 레이블 연결
  - 포커스 시각적 피드백

- [ ] **색상 대비**
  - 텍스트와 배경 색상 대비 확인
  - 접근성 기준 준수

### 6. 유지보수성 (Maintainability)

#### 6.1 코드 품질
- [ ] **코드 주석**
  - 복잡한 로직에 주석 추가

- [ ] **함수명 명확성**
  - 함수명이 기능을 명확히 표현

- [ ] **에러 처리**
  - 적절한 에러 처리 구현

- [ ] **코딩 스타일 일관성**
  - 일관된 코딩 스타일 유지

---

## 📊 우선순위별 요약

### 우선순위 1: 높은 우선순위 (즉시 구현)

#### 기능 요구사항
1. ✅ 시뮬레이터 실시간 계산 (완료)
2. ❌ 대시보드 현재 상태 요약 영역 입력
3. ❌ 대시보드 자산 & 수입 요약 합계 계산
4. ❌ 대시보드 데이터 저장 기능
5. ❌ 네비게이션 기능
6. ❌ 대시보드 엣지 케이스 처리

#### 비기능 요구사항
1. ❌ UI/UX 사용성 (5분 이내 파악)
2. ❌ Chrome 브라우저 호환성
3. ❌ Edge 브라우저 호환성

### 우선순위 2: 중간 우선순위 (단기 구현)

#### 기능 요구사항
1. ❌ 단계별 노후 계획 기능

#### 비기능 요구사항
1. ❌ 반응형 디자인
2. ❌ 레이아웃 검증
3. ❌ 글자 크기 검증
4. ❌ Firefox 브라우저 호환성
5. ❌ 네비게이션 링크 활성화

### 우선순위 3: 낮은 우선순위 (중기 구현)

#### 기능 요구사항
1. ❌ 건강 & 생활 관리 기능
2. ❌ 메모 & 할 일 관리 기능

#### 비기능 요구사항
1. ❌ 색상 검증
2. ❌ Safari 브라우저 호환성

---

## 📈 진행 상황

### 기능 요구사항
- **완료**: 1 / 15 항목 (6.7%)
- **미완료**: 14 / 15 항목 (93.3%)

### 비기능 요구사항
- **완료**: 0 / 10 항목 (0%)
- **미완료**: 10 / 10 항목 (100%)

### 전체
- **완료**: 1 / 25 항목 (4%)
- **미완료**: 24 / 25 항목 (96%)

---

## 📝 참고사항

### TDD 원칙
- GREEN 단계에서는 테스트를 통과시키는 **최소한의 코드**만 작성
- 리팩토링은 REFACTOR 단계에서 수행
- 각 기능 구현 후 즉시 테스트 실행하여 통과 확인

### 구현 순서 권장
1. 기능 요구사항 우선 구현 (핵심 기능)
2. 비기능 요구사항은 기능 구현 후 검증
3. 우선순위 1 항목부터 순차적으로 진행

---

**문서 작성일**: 2024년 12월 19일  
**최종 업데이트**: 2024년 12월 19일  
**상태**: 🟡 진행 중

---

*본 문서는 GREEN 단계에서 구현해야 할 항목을 기능/비기능 요구사항으로 분류한 문서입니다.*

